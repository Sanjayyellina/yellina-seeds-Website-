// Single source of truth for the hero's cornfield backdrop — used as both
// the visible page background (Hero.jsx) and the photo the particle logo's
// spheres/dots/trail reveal (HeroLeafParticles.jsx). Change the path here
// and both places pick it up automatically.
export const HERO_BG_IMAGE = '/images/photos/field-corn-rows-sunset.jpg'
